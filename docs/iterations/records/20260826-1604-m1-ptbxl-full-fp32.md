# Optimization Run: `20260826-1604-m1-ptbxl-full-fp32`

## Identity

- Stage: M1 full PTB-XL FP32 baseline
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: failed_data_integrity_gate
- Baseline: `20260826-1602-m1-ptbxl-async-retry`

## Problem and evidence

- The 21,798-file acquisition passed its own published SHA-256 checks, but it was not complete: the official `RECORDS` file contains 21,799 low-resolution paths and the prior parser omitted `records100/21000/21837_lr`.
- The model is intentionally small and hardware-oriented; this run establishes a reproducible reference before INT8 deployment.

## Optimization

- Method: the frozen TinyECGCNN (Conv1D/ReLU/MaxPool ×2, Conv1D/ReLU, global average, linear head), patient-level official folds, deterministic seed-specific runs on one GPU per candidate.
- Why this method: it matches the RTL operator plan and prevents architecture changes from being conflated with quantization or board effects.
- Alternatives considered: larger residual/attention models; deferred until the fixed-point and board path is stable.

## Frozen acceptance criteria

- PTB-XL 1.0.3, 21,388 labeled records, folds 1–8/9/10, no patient overlap.
- At least two seeds; each run records config, metrics, checkpoint hash, environment and GPU assignment.
- Metrics include macro/per-class AUROC, AUPRC and F1; no test-fold tuning.

## Execution

- Remote registry: private `LRX/12lead_fpga_20k_m1/runs/20260826-1602-m1-ptbxl-async-retry`.
- Candidate GPUs: freshly checked GPUs 0, 1 and 2 were idle; one seed per GPU, without stopping other processes.
- Planned seeds: 0, 1, 2; 8 epochs each.

## Results

- All three seed processes reached validation and failed with `FileNotFoundError` for `records100/21000/21837_lr.hea`; no checkpoint or valid baseline metrics were accepted.

## Decision

- Failed and superseded by `20260826-1613-m1-ptbxl-full-fp32-retry`. No benchmark claim is made from this run.
