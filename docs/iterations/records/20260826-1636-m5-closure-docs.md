# Optimization Run: `20260826-1636-m5-closure-docs`

## Identity

- Stage: M5 reproducibility/documentation closure
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: passed_documentation
- Baseline: `20260826-1634-m2-ptq-full-checkpoint`

## Problem and evidence

- The repository now contains complete-data training/quantization artifacts and two QN88 SRAM bitstream paths, but the README still describes the SDRAM/RTL gate as entirely future work.
- Future agents need discoverable entry points for the corrected downloader, QN88 SDRAM probe, QN88 INT8 smoke, and the explicit distinction between JTAG transfer and physical LED confirmation.

## Optimization

- Method: update root and FPGA README guidance with the current verified milestones, private remote artifact locations, SRAM-only commands, and remaining hardware-observation boundary; add this closure record to the iteration index.
- Why this method: reduce repeated setup and prevent future agents from treating incomplete or historical QN88P/PSRAM assumptions as current evidence.

## Frozen acceptance criteria

- Documentation points to the actual scripts, run IDs, hashes, and QN88/SDRAM contract.
- No raw dataset, checkpoint, vendor encrypted RTL, or bitstream is committed.
- README preserves the rule that every subsequent training/quantization/deployment round receives a new record before execution.

## Execution

- README, FPGA entry points, iteration index, and generated-build ignore rules were updated. Fresh Python/RTL/linter checks, QN88 SDRAM and inference Gowin builds, and SRAM-only programmer verification passed; no generated artifacts are staged.

## Results

- Documentation now points to the actual full-data/PTQ run IDs, private LRX paths, QN88 build scripts, and the LED-observation boundary. Current unverified items remain the physical SDRAM/inference LED states and model-sized ECG RTL deployment.

## Decision

- Documentation closure accepted after `git diff --check`, local tests, fresh builds, SRAM-only programming, commit, and push. Next permitted problem: capture independent LED/status evidence, then integrate model-derived golden vectors in a new run.
