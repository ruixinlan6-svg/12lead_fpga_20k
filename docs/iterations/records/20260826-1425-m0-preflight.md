# Optimization Run: `20260826-1425-m0-preflight`

## Identity

- Stage: M0 preflight
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: blocked_on_hardware_identity
- Baseline commit: `06b5d232b685f75b8b453a23b5b373226af66f35`

## Problem and evidence

- The project has a verified LED FPGA baseline, but no ECG model, I/O contract, or live tool/device evidence.
- Local board documents and the current Gowin project disagree on QN88/QN88P and SDRAM/PSRAM identity.
- GoAI/NPU use is therefore blocked until the physical device, memory type, host/control path, and installed tool versions are checked.

## Optimization

- Method: perform M0 read-only preflight; record local tool versions, project target, available documentation, SSH configuration visibility, and live JTAG/GPU evidence when accessible.
- Why this method: it resolves high-risk environment assumptions before data download, training, RTL design, synthesis, or programming.
- Alternatives considered: start with model training or copy the official GoAI example directly. Rejected because either path could target the wrong silicon or memory architecture.

## Frozen acceptance criteria

- Do not download datasets or modify remote GPU processes.
- Do not write FPGA configuration or persistent Flash.
- Record exact evidence and timestamps for each check.
- Mark hardware identity as confirmed only when independent evidence is available; otherwise leave it as a gate.

## Execution

- Commands/checks: local tool version checks; remote SSH identity/GPU checks; Gowin JTAG scan; serial enumeration; repository/project inspection.
- Environment: Windows PowerShell; repository `D:\project\gowin_project\0_fpga_test\test3`

## Results

- Local Python 3.14.4, Icarus 12.0, Gowin `gw_sh.exe`, and Programmer CLI are available.
- `ecg-gpu-server` authentication succeeded. Remote Python is 3.10.20 with PyTorch 2.7.1+cu128 and three CUDA devices. GPU 0 is the only currently clean candidate; GPU 1 requires a fresh check; GPU 2 has an active compute process and is excluded.
- JTAG scan succeeded and reported one GW2ANR-family device with ID `0x0000081B`, but did not resolve the exact QN88/QN88P package.
- No serial port was returned by the local WMI enumeration.
- Public/local evidence still conflicts between the EDA QN88P target and the Tang Nano QN88/SDRAM documentation. No memory read/write test was run.
- Artifacts: `contracts/hardware_contract.json`, `contracts/ecg_io_contract.json`, `docs/preflight-report.md`.
- Validation: both JSON contracts parse successfully; existing LED Icarus testbench passed; `git diff --check` passed.
- SHA-256: `hardware_contract.json` = `D88441C280B19C026DB33C9C01BC47976BC13345BA3F798F832F42CF7B845E49`; `ecg_io_contract.json` = `8719A3D537A4A76315ED4C42075B1A56B3A3E86DFBB7CC4227E16F7486110C71`; `preflight-report.md` = `6F9BF09D5F01743CB48AB20C3295C652B8165C6A9B6906A4CC199CA0CF4881FF`.

## Decision

- Decision: **return to hardware/tool setup**. M0 cannot be accepted until exact package and external memory type are independently confirmed. No GoAI or ECG training conclusion is authorized yet.
