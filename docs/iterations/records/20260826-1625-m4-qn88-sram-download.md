# Optimization Run: `20260826-1625-m4-qn88-sram-download`

## Identity

- Stage: M4 QN88 SRAM-only board deployment
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: passed_sram_transfer_led_pending
- Baseline: `20260826-1618-m4-qn88-sdram-probe`

## Problem and evidence

- The QN88 SDRAM probe bitstream passed synthesis/PnR and exists at `fpga/sdram_probe/build/qn88_sdram_probe/impl/pnr/qn88_sdram_probe.fs`.
- The first programming call was rejected locally because the Programmer CLI's `--device` choices use the family name `GW2AR-18C`; this occurred before any hardware write.

## Optimization

- Method: retry with the CLI-supported family selector `GW2AR-18C`, the explicit probe `.fs`, and `--program sram` only.
- Why this method: correct the tool's device-name contract while preserving the exact QN88-targeted bitstream; no persistent Flash operation is allowed.

## Frozen acceptance criteria

- Programmer reports successful SRAM configuration on the connected board.
- No `--program flash`, `--fiFile`, or other persistent-memory option is used.
- SDRAM pass remains a separate result: require an independent observation of LED0 init, LED1 pass, and LED2 no-error (or a future readable status path).

## Execution

- Corrected invocation used `--program sram --device GW2AR-18C --fs-file qn88_sdram_probe.fs`; the connected device was identified as `GW2AR-18C (0x0000081B)`.

## Results

- Programmer reached 100% on both the initial corrected transfer and a fresh post-build verification transfer, reporting `Successfully programmed FPGA (SRAM (Direct Run))`; reported status code was `0x00006020`. No Flash option was present. The latest SDRAM-probe `.fs` hash was `F9801EB7173A110D695AFCC80A1744BD82625D791063C2033A11FF1AC77A5243`.

## Decision

- SRAM transfer accepted. SDRAM read/write remains unverified until LED0 init, LED1 pass, and LED2 no-error are independently observed; no serial status device is currently enumerated.
