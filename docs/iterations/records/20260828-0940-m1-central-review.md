# Optimization Run: `20260828-0940-m1-central-review`

## Identity

- Run ID: `20260828-0940-m1-central-review`
- Stage: `data`
- Status: `completed`
- Started/finished: `2026-08-28T09:32:00+08:00 / 2026-08-28T09:40:00+08:00`
- Agent/operator: Codex central reviewer
- Baseline run: `20260828-090329-m1-reference-qrs`
- Git commit: `6e618a2cb509fb8eb252d2ac346bdf3c3694e776`
- Data version and split hash: synthetic-only; no LUDB or other ECG database was accessed
- Config/contract paths: `contracts/ec57_hybrid_io_contract.json`, `docs/superpowers/plans/2026-08-27-qn88-ec57-hybrid-implementation.md`
- Environment: Windows PowerShell, local Python; read-only audit plus unit-test execution

## Problem and evidence

- Observed problem: the submitted M1 run concluded `accept`, but the M1 plan requires fail-closed locked-data isolation, a valid 250 Hz polyphase resampler, the frozen adaptive QRS/SQI behavior, algorithm-generated evidence, and LUDB QRS Se/+P of at least 99.5%.
- Evidence from the baseline: the submitter reported 42/42 tests passing and explicitly marked LUDB as `not_evaluated`.
- Primary metric or failure point: passing tests did not cover several required negative cases, and the full M1 acceptance gate was not met.

## Optimization

- Method: reproduce the complete EC57 unit-test suite, inspect the implementation and tests against the frozen M1 contract, run adversarial locked-data path/context probes, and measure the 257 Hz to 250 Hz resampler response on deterministic sinusoids.
- Why this method: the review must distinguish test success from specification compliance and must independently challenge fail-closed claims.
- Alternatives considered and why not selected: no implementation repair or database download was performed because this run is a central audit, not a corrective implementation run.
- Expected mechanism: any reproducible locked-data bypass, unusable QRS-band resampling, hard-coded evidence, missing frozen QRS behavior, or absent LUDB gate prevents M1 closure.

## Frozen acceptance criteria

- Success threshold: all 42 tests reproduce; unknown usage/context and lexically equivalent locked paths are rejected; explicit records cannot create locked-data or patient-split leakage; the resampler preserves the useful QRS passband while meeting the timestamp contract; evidence is generated by executing the implementation; the frozen adaptive QRS and three-lead voting behavior is present; LUDB QRS Se/+P are each at least 99.5%.
- Failure/rollback threshold: any required negative case bypasses, the 5-25 Hz QRS band is materially attenuated by resampling, evidence is hard-coded, or LUDB is not evaluated.
- Fixed test set, thresholds and measurement conditions: repository state at the commit above; `python -m unittest discover -s tests/ec57 -p "test_*.py" -v`; 257 Hz input, 250 Hz output, unit-amplitude deterministic sinusoids after edge trimming.

## Execution

- Entry command or script: full EC57 unittest discovery; direct Python probes of `validate_dataset_configuration`, `validate_locked_roots`, and `resample_signal`.
- GPU/card or hardware connection used: none.
- Calibration/Golden sample manifest: none; synthetic signals only.
- Deviations from the plan: this was a review and did not modify M1 implementation files or access ECG data.

## Results

| Metric | Baseline claim | Central review | Delta | Comparable? |
|---|---:|---:|---:|---|
| EC57 unit tests | 42/42 | 42/42 | 0 | yes |
| Unknown-context locked-data probe | not tested | bypassed | blocking | yes |
| `..` locked-root probe | not tested | bypassed | blocking | yes |
| 25 Hz amplitude after 257->250 resampling | not reported | about 0.192 for unit input | blocking attenuation | yes |
| LUDB QRS Se/+P | `not_evaluated` | `not_evaluated` | none | yes |

- Per-class or per-layer findings:
  1. `build_registry.py` only enforces a fixed list of forbidden context spellings and does not fail closed for unknown values such as `training`.
  2. Windows locked-root comparison does not lexically normalize `..`; an equivalent path can bypass the descendant check.
  3. Explicit record assignments are not globally checked for patient cross-split leakage and can be written to a caller-selected split before locked-role handling.
  4. The 257->250 FIR design suppresses the QRS passband; the existing resampler test checks only a constant signal.
  5. QRS candidates are selected primarily from raw-amplitude thresholds; integrated energy is not used as the frozen adaptive signal/noise decision variable. Three-lead 2-of-3 voting, fixed 40-bit accumulation limits, and the specified derivative scaling are also absent.
  6. `generate_reference_evidence` hard-codes the synthetic peak list, zero mismatch, and zero leakage instead of deriving them from the detector and registry execution.
- Failed samples/first mismatch: `training + MIT-BIH Arrhythmia` was accepted; `D:\ecg\locked\other\..\mitdb\100.dat` was accepted against locked root `D:\ecg\locked\mitdb`; 25 Hz output amplitude was about 0.192.
- Logs and report paths: central review evidence is recorded in this file; no database-derived report was created.
- Artifact paths and SHA-256: submitted hashes were not contradicted; hash correctness does not resolve the functional blockers above.
- Unverified items: real LUDB metrics, WFDB `bxb`, long-record performance, model training, quantization, RTL integration, HIL, and board behavior.

## Decision

- Decision: `回到训练`
- Reason: per the M1 plan, failures must return to filtering, SQI/QRS, resampling, or data governance and must not advance to CNN training. The current code is useful as a synthetic interface skeleton but is not `M1-reference-accepted`.
- What changed in the project baseline: no implementation baseline changed; the M1 run is centrally classified as rejected for milestone closure.
- One primary question for the next run: can a corrective run close all fail-closed, resampling, adaptive QRS/voting, executable-evidence, and LUDB gates without consulting any locked database?
