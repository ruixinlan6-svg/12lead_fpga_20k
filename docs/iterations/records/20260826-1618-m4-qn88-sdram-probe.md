# Optimization Run: `20260826-1618-m4-qn88-sdram-probe`

## Identity

- Stage: M4 QN88 embedded SDRAM volatile probe
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: passed_build_sram_download_led_pending
- Baseline: `d1222e9`

## Problem and evidence

- QN88 was confirmed by the user; the local Gowin installation contains the encrypted `SDRC_EMB` IP for `GW2AR-LV18QN88C8/I7` and documents a 64-Mbit embedded SDRAM path.
- The first headless build reached synthesis after adding an auditable `sdrc_defines.v` configuration header and using the generated module name, but physical constraints failed because external ports defaulted to LVCMOS18 while embedded-SDRAM banks are locked to 3.3 V.

## Optimization

- Method: keep the embedded SDRAM unassigned and explicitly constrain only the clock, reset, and LED user ports as `IO_TYPE=LVCMOS33`; retain the QN88 device/version and volatile pattern FSM.
- Why this method: resolve a board-bank voltage conflict without touching the SDRAM route or changing the memory protocol.

## Frozen acceptance criteria

- Gowin synthesis/PnR targets exact `GW2AR-LV18QN88C8/I7` and emits a bitstream.
- The probe performs four 32-word write/read bursts with distinct patterns and reports init/pass/error on active-low LEDs.
- First programming is SRAM only; no QSPI Flash write.
- A downloaded bitstream is not called a memory-pass result unless the physical LED/status observation is independently confirmed.

## Execution

- Probe sources: `fpga/sdram_probe/`; vendor encrypted RTL remains in the local Gowin installation.
- Initial result: synthesis passed; PnR stopped at `CT1136` Bank_VCCIO conflicts. The LVCMOS33 rerun then passed PnR and generated `qn88_sdram_probe.fs`.

## Results

- PnR and bitstream generation passed. The corrected SRAM invocation completed at 100% on `GW2AR-18C`; the first selector error was rejected before touching hardware and is retained in `20260826-1625-m4-qn88-sram-download`.
- Latest regenerated bitstream SHA-256: `F9801EB7173A110D695AFCC80A1744BD82625D791063C2033A11FF1AC77A5243`.

## Decision

- Build and SRAM transfer accepted; board memory result remains pending. Do not set `read_write_test_passed=true` until an independent LED/status observation pass.
