# Optimization Run: `20260826-1908-m2-model-verify`

## Identity

- Run ID: `20260826-1908-m2-model-verify`
- Stage: `quant`
- Status: `completed`
- Started/finished: 2026-08-26 19:08 Asia/Shanghai / 2026-08-26 19:18 Asia/Shanghai
- Agent/operator: Codex
- Baseline run: `20260826-1634-m2-ptq-full-checkpoint`
- Git commit: `376e3ade1333f12fc67e6166847a54e0f0044f58` (project baseline used during execution)
- Data version and split hash: PTB-XL 1.0.3; registry `28cc21606bf2c8264df670e3f932b0fbcb27324d8f053970814a4102e65db973`; manifest `b1ee5de4ee2efd25cb76797444d89c19e5d21c25678532ea347fca6459b2aad0`
- Config/contract paths: remote `C:/Users/Administrator/Desktop/LRX/12lead_fpga_20k_m1/runs/20260826-1634-m2-ptq-full-checkpoint/seed1/`; `train/ptq_int8.py`
- Environment: remote `ecg-gpu-server`, `lrx_train`, CPU-only PTQ evaluator; GPU occupancy was checked and no training GPU was used; local Icarus simulation

## Problem and evidence

- Observed problem: the repository records a complete-checkpoint INT8 PTQ result, but the current turn needs fresh evidence that the checkpoint, registry, quantization contract, golden vector and software evaluator still agree. The existing QN88 board smoke is an arithmetic slice and is not a model verification.
- Evidence from the baseline: seed1 validation macro-AUROC `0.8728871482` FP32 vs `0.8724959775` INT8; test macro-AUROC `0.8578462158` vs `0.8577429873`. Existing golden vectors are remote/private and no model-sized FPGA top-level is present in this checkout.
- Primary metric or failure point: exact artifact/metric reproducibility, model output parity on the fixed first validation sample, and separation of model-level evidence from the existing operator/board smoke.

## Optimization

- Method: rerun the frozen static per-tensor symmetric INT8 PTQ script from the same complete seed1 checkpoint and registry into a new immutable run directory; compare fresh metrics, scales, and golden outputs with the recorded baseline; then run the existing RTL arithmetic regression and inspect the QN88 inference smoke boundary.
- Why this method: it changes no model architecture or quantizer, isolates reproducibility, and tests the exact artifacts that later model-derived RTL integration must consume.
- Alternatives considered and why not selected: retraining would add unrelated variance; full FPGA model inference is not possible until a model-sized RTL top and weight/input protocol exist; Flash programming is outside scope.
- Expected mechanism: same inputs, checkpoint and quantizer should reproduce metrics and golden outputs bit-for-bit (apart from explicitly recorded serialization differences).

## Frozen acceptance criteria

- Success threshold: remote PTQ exits 0; complete registry has no missing records; fresh checkpoint/registry hashes match the baseline; validation/test macro-AUROC and macro-F1 deltas match baseline within `1e-6`; fixed first validation sample `input_int8` and `int8_logits` match the prior golden vector element-wise; RTL Level-1 regression exits 0.
- Failure/rollback threshold: any hash mismatch, missing data, non-deterministic metric beyond `1e-6`, first-sample mismatch, or RTL failure sends the decision back to the responsible stage; no board claim is made from a failure.
- Fixed test set, thresholds and measurement conditions: PTB-XL official patient-level split; 2,048 train calibration records and all validation/test records; CPU evaluator with seed 0 as in the frozen script; local Icarus testbenches only; QN88 SRAM smoke remains a separate non-model check.

## Execution

- Entry command or script: remote `train/ptq_int8.py --calibration-samples 2048 --eval-samples 2158`, followed by local Icarus `tb_conv1d_mac_int8`, `tb_requantize_clip`, and the QN88 inference-smoke testbench.
- GPU/card or hardware connection used: `ssh ecg-gpu-server`; `nvidia-smi` showed GPU 0/2 idle and only desktop processes on GPU 1; no training process or FPGA programming was started.
- Calibration/Golden sample manifest: first validation record in the frozen registry; remote output `C:/Users/Administrator/Desktop/LRX/12lead_fpga_20k_m1/runs/20260826-1908-m2-model-verify`.
- Deviations from the plan: none affecting results; the QN88 smoke was simulation-only because it is not model-sized.

## Results

| Metric | Baseline | This run | Delta | Comparable? |
|---|---:|---:|---:|---|
| Validation macro-AUROC | 0.8724959775 INT8 | 0.8724959775 | 0 | yes |
| Test macro-AUROC | 0.8577429873 INT8 | 0.8577429873 | 0 | yes |
| Validation macro-F1 | 0.6560869986 INT8 | 0.6560869986 | 0 | yes |
| First-sample integer/golden parity | recorded remote vector | exact array equality for 4 arrays | 0 | yes |
| RTL Level-1 | passed baseline | both unit tests PASS | — | yes |

- Per-class or per-layer findings: all five per-class FP32/INT8 metrics in `metrics_int8.json` match the baseline JSON exactly; the fixed Golden arrays have shapes `(1,12,1000)`, `(1,12,1000)`, `(1,5)`, `(1,5)` and all are element-wise equal. Local RTL output was `[PASS] conv1d_mac_int8 self-check`, `[PASS] requantize_clip self-check`, and `PASS: known vector dot=240 requantized=120 led=011100` for the QN88 smoke testbench.
- Failed samples/first mismatch: none in the fixed Golden comparison; input range was `[-0.48779997, 0.25380000]`, INT8 input range `[-62, 32]`, and both logit arrays were finite.
- Logs and report paths: remote run directory above; local RTL test output captured in this turn; no raw data copied into the repository.
- Artifact paths and SHA-256: `golden_vectors.npz` `2FAF8B77D7BFC03BF52C4AF891F2D71007BDD8B92CDE079B58CE1A4B24C08877`; `metrics_int8.json` `86AC6A49E03773901508EA1F3A172EFC675EA04EC2494413B3111AF5DA9D4C64`; `quantization_contract.json` `6FB4B332DBBE42F2FEC25A4256F402185F49E7CAAF9FEF2425AA0C59A9686E76`; `weights_int8.pt` `2AF6FF4FC65E2C9F9B4A70714B6776CE85D7E3122C5C8CBFC9ECD12E3E7E6D9F`.
- Unverified items: full model-sized RTL/GoAI graph, layer-by-layer integer Golden tensors beyond the first sample, model weights in SDRAM, QN88 end-to-end ECG logits, clinical/generalization claims.

## Decision

- Decision: `accept`
- Reason: the complete-checkpoint PTQ rerun reproduced the baseline metrics, contract, weights and Golden vectors exactly, and the frozen INT8 RTL operators plus QN88 smoke simulation passed. This accepts the software model/quantization verification only; it does not accept full FPGA model deployment.
- What changed in the project baseline: documentation and iteration evidence only; no model or RTL source change.
- One primary question for the next run: can the verified integer graph be connected layer-by-layer to a QN88 model-sized buffer/weight protocol without changing the frozen quantization semantics?
