# Optimization Run: `20260826-1615-m3-int8-level1`

## Identity

- Stage: M3 Level-1 INT8 arithmetic
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: passed_level1
- Baseline commit: `5e6777d`

## Problem and evidence

- QN88 is confirmed and the deployment route is self-owned INT8 RTL, but no reusable bit-exact requantization or Conv1D MAC unit exists in the public project.
- The full PTB-XL M1 download is still running remotely and cannot yet provide model-derived vectors.

## Optimization

- Method: implement a small signed INT8 dot-product stream and signed requantize/clip unit with independent Icarus self-checking testbenches.
- Why this method: establish arithmetic and rounding semantics before adding SDRAM traffic, model-specific weights, or board protocols.
- Alternatives considered: import the previous PSRAM/24-layer RTL or start from a full network. Rejected because both encode the wrong memory/label assumptions for QN88.

## Frozen acceptance criteria

- Signed two's-complement arithmetic; explicit saturation to [-128, 127].
- Symmetric nearest rounding for nonzero right shifts; shift 0 is exact.
- MAC result must include the final sample on `in_last` and assert one-cycle `done`.
- Testbenches must fail on any mismatch and pass with Icarus Verilog.

## Execution

- Local RTL and testbench implementation: completed in `fpga/rtl/` and `fpga/tb/`.
- Model-derived vectors: pending M2.

## Results

- `tb_requantize_clip`: PASS; signed shift/round/saturation vectors match.
- `tb_conv1d_mac_int8`: PASS; final `in_last` product is included and one-cycle `done` is observed.
- Gowin RTL linter: 0 fatal errors, 0 warnings for `fpga/rtl`.
- One initial test-vector expectation was corrected in the testbench after checking signed arithmetic; RTL semantics were unchanged.
- SHA-256: `requantize_clip.sv` = `77720C21761AFE32C3EF8CAA0A3819D88BC0007793B902F56232C626E662F83D`; `conv1d_mac_int8.sv` = `1EAEC7FAF1DDB730FB9347299386549F5577D258FCF173870EAB7B7E0E0D2CD5`; `tb_requantize_clip.sv` = `20C0FEBE300A9A15C21C756F7D8209F1B75E226C9E2E8D75C91B287B28FABB8D`; `tb_conv1d_mac_int8.sv` = `DFD970267E8C1F38FA5146546E6866674B9188AF98E68396659E0DA4D90DFC4A`.

## Decision

- Accept Level-1 arithmetic. Freeze these semantics for M2 model-derived golden vectors; any change requires a new run ID.
