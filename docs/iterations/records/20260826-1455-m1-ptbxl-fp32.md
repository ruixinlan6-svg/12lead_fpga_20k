# Optimization Run: `20260826-1455-m1-ptbxl-fp32`

## Identity

- Stage: M1 data registry and FP32 baseline
- Date: 2026-08-26
- Owner: Codex implementation agent
- Status: aborted_superseded_by_parallel_download
- Baseline commit: `5e6777d`

## Problem and evidence

- QN88/SDRAM is confirmed, but the repository has no reproducible PTB-XL registry, patient-level manifest, model definition, or FP32 metric runner.
- Remote GPU environment is available, but its existing ECG caches are for other datasets/tasks and must not be silently reused.

## Optimization

- Method: add a minimal, hardware-oriented PyTorch 1D-CNN baseline with deterministic PTB-XL registry/split generation, explicit lead order, and JSON metrics.
- Why this method: it creates the software reference needed by later INT8 and RTL stages while keeping the operator set limited to Conv1D, ReLU, pooling, global average pooling, and a small dense head.
- Alternatives considered: reuse unrelated remote caches or start from the older 24-layer PSRAM/NPU experiment. Rejected because their labels, sampling, and hardware assumptions do not match PTB-XL/QN88.

## Frozen acceptance criteria

- Dataset source/version/license and file hashes must be recorded before training.
- Patient-level official folds; no test-fold tuning.
- Lead order and preprocessing must match `contracts/ecg_io_contract.json`.
- At least two seeds for a claimed baseline; all configs/logs/checkpoints receive SHA-256 hashes.
- No dataset, checkpoint, or raw ECG is committed to the public repository.

## Execution

- Local implementation and syntax checks: pending.
- Remote dataset download: sequential attempt started, then stopped by the owner after throughput proved unsuitable; partial files are retained for hash-checked reuse.
- Allowed GPUs: only freshly confirmed idle GPU(s), one candidate per card.

## Results

- The metadata-only download and registry generation succeeded. The first sequential waveform download was intentionally aborted before completion; no model training was started.

## Decision

- Decision: **supersede with a new run** using bounded parallel HTTP downloads; the method changed and therefore receives a new `run_id`.
